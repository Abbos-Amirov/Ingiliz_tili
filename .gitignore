node_modules/
.env
.env.local
.next/
dist/
build/
*.log
.DS_Store
*.apk

# Auto-generated by tts.service.ts when an admin adds/edits a word — lives on
# the server (bind-mounted, see docker-compose.yml), not version-controlled.
backend/public/audio/

# Firebase service account key (push.service.ts / config/firebase.ts) — a
# real secret, deployed to the server manually like the other API keys.
backend/secrets/
